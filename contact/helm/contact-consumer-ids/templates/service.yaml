apiVersion: v1
kind: Service
metadata:
  name: {{ include "contact-consumer-ids.fullname" . }}
  labels:
    {{- include "contact-consumer-ids.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "contact-consumer-ids.selectorLabels" . | nindent 4 }}
